server:
  port : 5001
  
# ==============================================================
# = Proprietà Livello Log
# = Default ERROR, WARN e INFO 
# = Levels: TRACE, DEBUG, INFO, WARN, ERROR, FATAL, OFF
# ============================================================== 

logging:
  file:
    name: mediaw.log
    max-size: 1MB
  pattern:
    file: "%d{ dd-MM-yyyy } [%thread] %-5level %logger{36} - %msg%n"
    #console: "%d{dd-MM-yyyy HH:mm:ss} - %msg%n"
    
  # Livello del Log generale
  level:
   root: INFO

mediaworld:
  uri: https://www.mediaworld.it/api/v1/stores/itemsAvailability/
  cordinate: https://api.opencagedata.com/geocode/v1/json?
  key: a337393afafc42e8a36dd548a94b7008
  state: italy