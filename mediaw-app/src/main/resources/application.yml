server:
  port : 5001
  
# ==============================================================
# = Proprietà Livello Log
# = Default ERROR, WARN e INFO 
# = Levels: TRACE, DEBUG, INFO, WARN, ERROR, FATAL, OFF
# ============================================================== 

logging:
  file:
    name: mediaw.log
    max-size: 1MB
  pattern:
    file: "%d{ dd-MM-yyyy } [%thread] %-5level %logger{36} - %msg%n"
    #console: "%d{dd-MM-yyyy HH:mm:ss} - %msg%n"
    
  # Livello del Log generale
  level:
    com.freeinfo.mediaw.mediawapp.connector.OpenCageDatqaFeignClient: DEBUG
    com.freeinfo.mediaw.mediawapp.connector.MediaWorldFeignClient: DEBUG
    root: INFO
    config:

client:
  opencagedata:
    base_url : https://api.opencagedata.com/geocode/v1/json
    token: a337393afafc42e8a36dd548a94b7008
    state: it

  mediaworld:
    base_url: https://www.mediaworld.it/api/v1/stores/itemsAvailability/